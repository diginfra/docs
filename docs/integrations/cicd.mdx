---
slug: cicd
title: Add to CI/CD
---

import useBaseUrl from '@docusaurus/useBaseUrl';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

export const Grid = ({logos}) => {
    const items = logos.map((e, i) => {
      return (<CIBox key={i} {...e}/>)
    })
    return (
      <div
        className="ci-grid"
        >
        {items}
      </div>
    )
};

export const CIBox = ({image, link, maxHeight, newWindow, text}) => (
  <a
    className="ci-box"
    href={link}
    >
    <img
      src={image}
      target={newWindow ? "_blank" : null}
      style={{
        margin: "0 auto",
        maxHeight: maxHeight ?? "2rem"
      }}
      />
      {text && (<div className="ci-subtitle">{text}</div>)}
  </a>
)

Use one of our many integrations so your engineering team can see cost estimates and FinOps best practices in pull requests **before making changes**. This provides your team with a safety net as people can discuss FinOps as part of the workflow.

<img src={useBaseUrl("img/screenshots/actions-pull-request.png")} alt="Diginfra pull request comment" />

If you run into any issues, please join our [community Slack channel](https://www.diginfra.khulnasoft.com/community-chat), we'll help you very quickly 😄

## Source control integrations (recommended)

Diginfra supports direct integration with GitHub and GitLab. We recommend these integrations as they are much simpler to setup and faster to run.

<Grid logos={[
    {
      image: "/docs/img/services/github.svg",
      text: "GitHub.com, Enterprise Cloud & Enterprise Server",
      link: "/docs/integrations/github_app",
      newWindow: false
    },
    {
      image: "/docs/img/services/azure-repos.svg",
      text: "Azure Repos (Azure DevOps Services)",
      link: "/docs/integrations/azure_repos_app",
      newWindow: false
    },
    {
      image: "/docs/img/services/gitlab.svg",
      text: "GitLab.com & self-managed",
      link: "/docs/integrations/gitlab_app",
      newWindow: false
    },
]}/>

## CI/CD integrations

The following integrations can also be used to post pull request comments.

<Grid logos={[
    {
      image: "/docs/img/services/github-actions.png",
      link: "https://github.com/diginfra/actions",
      newWindow: true
    },
    {
      image: "/docs/img/services/gitlab-ci.png",
      link: "https://gitlab.com/diginfra/diginfra-gitlab-ci",
      newWindow: true
    },
    {
      image: "/docs/img/services/atlantis.png",
      link: "https://github.com/diginfra/diginfra-atlantis/",
      newWindow: true
    },
    {
      image: "/docs/img/services/azure-pipelines.png",
      link: "https://github.com/diginfra/diginfra-azure-devops",
      newWindow: true
    },
    {
      image: "/docs/img/services/jenkins.svg",
      link: "https://github.com/diginfra/diginfra-jenkins/",
      newWindow: true
    },
    {
      image: "/docs/img/services/bitbucket-pipelines.png",
      link: "https://bitbucket.org/diginfra/diginfra-bitbucket-pipeline/src/master/",
      newWindow: true
    },
]}/>

<br/>

For other CI/CD systems, the GitLab CI integration above is a good example to follow. Please [email us](mailto:hello@diginfra.khulnasoft.com) if you need help. We maintain the [`diginfra/diginfra:ci-0.10`](https://hub.docker.com/r/diginfra/diginfra/tags) Docker image that can be used in other CI/CD integrations. This uses the latest patch version of 0.10 to pick up bug fixes and new resources.

To test the bleeding edge version (not recommended for production deployments), use the [`diginfra/diginfra:ci-edge`](https://hub.docker.com/r/diginfra/diginfra/tags) image or the [preview build](https://github.com/diginfra/diginfra/releases/tag/preview).
